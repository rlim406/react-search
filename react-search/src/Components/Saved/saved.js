import React from "react";
import "./saved.css";
import { Container, Header, Row } from 'reactstrap';

const Saved = props => (

  <Container className="saved-articles main-content-section" id="articles">
    <Header>
      <h2 class="card-header">Saved articles</h2>
    </Header>
    <Row>
    </Row>
  </Container>
);

export default Saved;